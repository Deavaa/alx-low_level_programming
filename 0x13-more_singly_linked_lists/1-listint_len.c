#include "lists.h"
#include <stdio.h>

/**
 * listint_len - no of elements
 * @h: pointer to list
 * Return: no of list
 */

size_t listint_len(const listint_t *h)
{
	const listint_t *tmp;
	int = 0;

	tmp = h;
	while (tp)
	{
		i++;
		tmp = tmp->next;
	}
	return (i);
}
