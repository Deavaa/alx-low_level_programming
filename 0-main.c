#include "main.h"

/**
 * main -tests functio that prints if iteger is positiv or negative
 * Return: 0
 */

int main(void)
{
	int i;

	i = 98;
	if (i == 0)
	{
		printf("%d is zero",i);
	}

	return (0);
}
